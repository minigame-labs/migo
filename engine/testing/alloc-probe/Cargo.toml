[package]
name = "migo-alloc-probe"
version = "0.1.0"
edition = "2024"
publish = false
description = "Counting allocator and burst assertion for the steady-state allocation gate. Test-only."

[lib]
name = "migo_alloc_probe"

[dependencies]
