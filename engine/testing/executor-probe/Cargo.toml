[package]
name = "migo-executor-probe"
version.workspace = true
license.workspace = true
edition = "2024"
publish = false
description = "Occupies a shared executor with a CPU-bound step and requires a co-tenant task to run anyway. Test-only."

[lib]
name = "migo_executor_probe"

[dependencies]
parking_lot.workspace = true
tokio = { workspace = true, features = ["rt", "rt-multi-thread"] }
