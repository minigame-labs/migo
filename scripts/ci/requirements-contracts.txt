# Linux x86_64 quality-gate dependency. The workflow selects CPython 3.11,
# so this hash binds the exact manylinux wheel rather than accepting whichever
# PyPI artifact happens to satisfy a version range.
PyYAML==6.0.3 \
    --hash=sha256:b8bb0864c5a28024fac8a632c443c87c5aa6f215c0b126c449ae1a150412f31d
